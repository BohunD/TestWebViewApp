<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TestWebViewApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#4CAF50</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Hide the navigation bar (bottom bar with buttons) -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- Other style attributes -->
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TestWebViewApp" parent="Base.Theme.TestWebViewApp" />
    <style name="StandardSymbolStyle" >
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
    </style>
</resources>